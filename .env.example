# Required only when running review with --claude
ANTHROPIC_API_KEY=sk-ant-...

# Optional: choose a provider without a CLI switch (codex is the default)
# REVIEW_AI_PROVIDER=codex

# Optional model overrides
# REVIEW_CODEX_MODEL=<model-id>
# REVIEW_MODEL=claude-opus-5
